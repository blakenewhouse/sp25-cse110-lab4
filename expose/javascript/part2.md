1. It will print '3' because this is the last value of i from the loop and it is in the function scope of var.
2. It will print '150' because this is the last value of discountedPrice from the loop and it is in the function scope of var.
3. It will print '150' because this is the last value of finalPrice from the loop.
4. It will print '[50, 100, 150]' as this is the value of discounted after the for loop.
5. It will error, because the let variable i only has scope inside of the for loop.
6. It will error, because the let variable discountedPrice only has scope inside of the for loop.
7. It will print '150' because that is what finalPrice gets set to last, and it is within the scope.
8. It will print '[50, 100, 150]' because that is what discounted gets set to, and it is within the scope.
9. It will error, because the let variable i only has scope inside of the for loop.
10. It will print '3', because that is the length of prices.
11. It will print '[50, 100, 150]', because you can still push to a const array.
12. notation:
    A. student.name
    B. student['Grad Year']
    C. student.greeting()
    D. student['Favorite Teacher'].name
    E. student.courseload[0]
13. Arithmatic:
    A. 5
    B. 1
    C. 3
    D. 3
    E. 4
    F. 0
    G. NaN
    H. NaN
14. Comparisons:
    A. True
    B. True
    C. True
    D. False
    E. False
    F. True
15. The double equal(==) checks fo equality while the triple equal(===) checks without type conversion.
16. code in part2-question16.js
17. [2,4,6] - The function takes in the array [1,2,3], and the function doSomething that doubles a number. since we use callback inside of our push, it uses the callback to the function doubler and doubles each number in the array.
18. code in part2-question18.js
19. '1 4 3 2'
20. 